{
  "extends": ["//"],
  "tasks": {
   "build": {
      "outputs": ["dist/**"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "src/**/*.ts",
        "tsconfig.json",
        "package.json",
        "eslint.config.ts",
        "tsup.config.ts"
      ]
    }
  }
}
